# Tier-2 public feed (feed/) — API + sanitizer only.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27          # required by fastapi.testclient.TestClient

# scraper_job.py additionally imports omnipong_scraper.py / browser_manager.py
# from the repo root, which need the ROOT requirements.txt installed too
# (playwright, sqlalchemy, aiosqlite, beautifulsoup4, python-dotenv). Run:
#   pip install -r requirements.txt -r feed/requirements.txt
#   playwright install chromium
